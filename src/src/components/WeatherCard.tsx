import React from "react";
import { IWeatherData } from "../models/models";

interface WeaterDataCardProps {
  weatherData: IWeatherData;
}

export function WeatherCard({ weatherData }: WeaterDataCardProps) {
  return (<div>
   {

   }
  </div>);
}
